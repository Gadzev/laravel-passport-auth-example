# Simple laravel app that uses passport for authentication

## Instalation

```
git clone https://github.com/Gadzev/laravel-passport-auth-example.git
cd laravel-passport-auth-example
composer install
cp .env.example .env
php migrate
php artisan serve
```
